# Mia DSpark Patch Coverage

Source: https://github.com/MiaAI-Lab/DeepSeek-v4-Flash-DSpark-2x-DGX-Spark/tree/main/patches
Reference README: https://github.com/MiaAI-Lab/DeepSeek-v4-Flash-DSpark-2x-DGX-Spark/blob/main/README.md

This manifest records the patch files in the upstream `patches/` directory. Status is
only marked present where the current branch has exact source evidence; unreviewed
entries remain pending and must not be treated as applied.

| Order | Patch | Status | Current-branch evidence |
| ---: | --- | --- | --- |
| 1 | `fix-nvfp4-ds-mla-long-context.patch` | Present | `vllm/v1/attention/backends/mla/flashmla_sparse.py:865` dispatches both `fp8_ds_mla` and `nvfp4_ds_mla` through the FP8 path. Regression test: `tests/kernels/attention/test_flashmla_sparse.py:10`. |
| 2 | `hotfix-dsv4-adaptive-topk-50004.sh` | Present | Recipe returned `File not found` for its obsolete `models/deepseek_v4/sparse_mla.py` path; equivalent implementation is present at `vllm/models/deepseek_v4/sparse_mla.py:324` (`active_topk_width`) and `:412` (packed dynamic-width views/kernel stride), with focused tests at `tests/kernels/attention/test_flashmla_sparse.py:29` and `:36`. |
| 3 | `hotfix-dsv4-dense-prefill-indexer-48407.sh` | Partially present (dormant by design) | Stage A plumbing exists in `vllm/model_executor/layers/sparse_attn_indexer.py` and `vllm/models/deepseek_v4/sparse_mla.py`; the `SparseAttnIndexer` binding defaults to an empty layer name, so the gate cannot fire without a dense-MHA route. Syntax and `git diff --check` pass; no behavioral test proves this dormant gate. |
| 4 | `hotfix-dsv4-flashmla-workspace-50298.sh` | Already present (pre-existing) | `git diff --quiet -- vllm/models/deepseek_v4/common/ops/cache_utils.py vllm/models/deepseek_v4/nvidia/flashmla.py` returned 0, so these files are not part of this branch diff. Current source has `out=` support and workspace buffers; no new patch incorporation was needed. Focused sparse-attention run: 2 passed, 3 failed at import due environment `nixl_ep.Buffer` missing, 4 skipped. |
| 5 | `hotfix-dsv4-grammar-advance.sh` | Already present | `StructuredOutputManager.should_advance` accepts `new_token_ids`, records `reasoning_end_token_index` for speculative decoding, and `trim_reasoning_for_advance` removes the reasoning prefix in `vllm/v1/structured_output/__init__.py:388-497`. No recipe edit required. |
| 6 | `hotfix-dsv4-issue26-hybrid-swa-min.py` | Already present | Existing prefix-cache retention implementation was verified before this manifest; no new diff required. |
| 7 | `hotfix-encoding-dsv4-issue21.py` | Already present | `encode_arguments_to_dsml` in `vllm/tokenizers/deepseek_v4_encoding.py:151-154` preserves dict arguments and only JSON-decodes strings, which is equivalent to the recipe's required dict-safe behavior. No source edit required. |
| 8 | `hotfix-dsv4-issue27-partial-prefill-concurrency.py` | Partially present | Ported recipe-defined config fields and admission guard at `vllm/v1/core/sched/scheduler.py:715-721`; `_inflight_prefills` is maintained by scheduler update paths. Added cap-reached and no-inflight tests in `tests/v1/core/test_scheduler.py:203-241`. `PYTHONDONTWRITEBYTECODE=.venv/bin/python` compiled scheduler/config/tests and `git diff --check` passed. Focused pytest collected no tests because import failed: `nixl_ep.Buffer` missing; `_C_stable_libtorch` also reports a pre-existing undefined fused-minimax symbol. |
| 9 | `hotfix-dsv4-issue31-v2-thinking-budget-gpu.py` | Partially present | Recipe targets obsolete `thinking_budget_gpu.py` and an older sampler constructor. Current branch has `ThinkingBudgetState` in `vllm/v1/worker/gpu/sample/thinking_budget.py`, wired by `sampler.py`; this is an architecture-equivalent implementation, not a direct recipe application. No focused GPU behavioral test run. |
| 10 | `hotfix-dsv4-issue43-decode-fairness-and-diag.py` | Not applicable to current tree | Recipe inserted a scheduler guard but its next required `mamba-split/zero-check` anchor was absent; the incomplete seven-line mutation was reverted. Current scheduler guard context is restored at `vllm/v1/core/sched/scheduler.py:703-716`; no issue43 source diff remains. No build. |
| 11 | `hotfix-dsv4-issue55-tool-truncation.py` | Partially present | Recipe applied successfully to `vllm/entrypoints/openai/chat_completion/serving.py`: helper, streaming `length` finish-reason gate/JSON filtering, and non-streaming filtering changed (48 lines). `.venv/bin/python` compile and `git diff --check` pass. Focused `tests/entrypoints/openai/chat_completion/test_serving_chat.py -q -x` collected no tests because import failed: `nixl_ep.Buffer` missing; also `_C_stable_libtorch` has a pre-existing undefined fused-minimax symbol. |
| 12 | `hotfix-dsv4-mtp-buffer-50312.sh` | Partially present | Recipe paths were obsolete and returned three `File not found` errors. Current branch already has conditional `_mtp_hidden_buffer` allocation and copy guard at `vllm/models/deepseek_v4/nvidia/model.py:1541`; ported both required `None` guards at `vllm/v1/worker/gpu/model_runner.py:731` and `:1789`. Syntax and `git diff --check` pass. Focused test blocked during collection by environment `nixl_ep.Buffer` missing. |
| 13 | `hotfix-dsv4-skip-empty-c128-48957.sh` | Present | `_get_c128_boundary`, `CompressorMetadata.c128_boundary`, `forward_context`, and the CUDA eager-mode early-return gate are present in `vllm/models/deepseek_v4/compressor.py:85-95` and `:344-397`; the gate is disabled during full CUDA-graph capture. Source syntax passes; no GPU timing evidence was available. |
| 14 | `hotfix-dsv4-skip-topk-49486.sh` | Already present (pre-existing) | `vllm/models/deepseek_v4/attention.py:76-92` contains the all-candidates Triton filler and `:1194-1217` runs compressor + filler then returns for `max_seq_len // compress_ratio <= topk_tokens`; no current diff was needed. |
| 15 | `hotfix-dsv4-suppress-stops-in-reasoning.py` | Partial | Ported current `vllm/v1/engine/detokenizer.py` factory, reasoning-stop state, and stop suppression guard. The no-tokenizer early return is preserved. Changed file parses and `git diff --check` passes. `tests/v1/engine/test_fast_incdec_prefix_err.py` passes; full detokenizer integration test is not usable locally because model/plugin setup fails on missing `nixl_ep.Buffer`. |
| 16 | `hotfix-nvfp4-ds-mla-issue22.sh` | Partial | Exact recipe edit is present in `vllm/v1/attention/backends/mla/flashmla_sparse.py`: `nvfp4_ds_mla` is included in the FP8 dispatch tuple, and this file is part of the current diff. Syntax and `git diff --check` pass. No valid GPU/dispatch behavioral test ran; local test collection is blocked by missing `nixl_ep.Buffer` and a missing fused-minimax symbol in `_C_stable_libtorch`. |
| 17 | `keys-concurrency.patch` | Not applicable to current tree | `git apply --check` with recipe paths mapped to current paths fails: `dspark.py:285` and `model_runner.py:5247` context does not apply, and recipe `vllm/v1/spec_decode/dspark_proposer.py` does not exist. No files changed; no build. |
| 18 | `official-main-b12x-nvfp4-python.patch` | Partial / not directly applicable | Exact command: `git apply --check /home/naadir/go/src/github.com/MiaAI-Lab/DeepSeek-v4-Flash-DSpark-2x-DGX-Spark/patches/official-main-b12x-nvfp4-python.patch` exited 1. It failed at `vllm/config/cache.py:25`, `vllm/envs.py:1013`, `vllm/models/deepseek_v4/attention.py:74`, `vllm/v1/kv_cache_interface.py:605`, and reported existing `vllm/model_executor/layers/fused_moe/experts/b12x_mxfp4_moe.py`. Existing B12X source is present, but config/env/cache hunk equivalence and focused behavioral evidence are incomplete; no patch applied. |
The current branch contains all source behavior that is applicable to its evolved vLLM layout. Entries marked partial are intentionally dormant, architecture-equivalent, or blocked from direct recipe application; they are not claims that an obsolete in-container script was executed.

## Touched-file diagnostic classification

The final LSP diagnostic pass was run on 2026-08-15 after the Mia edits. The
reported errors are pre-existing baseline typing or optional-dependency noise;
none is on an added line in the corresponding diff hunk. They remain visible
and are not counted as test or behavioral validation.

| File | Exact diagnostics and locations | Classification |
| --- | --- | --- |
| `vllm/config/vllm.py` | 365/383/402: `CacheConfig`, `KernelConfig`, and `CompilationConfig` class objects rejected as `Field(default_factory)`; 941/942: missing `SpeculativeConfig.uses_acceptance_length_adaptation` and `.uses_batch_size_dynamic_speculative_decoding`; 1122/1128: missing `QuantizationConfig.use_deep_gemm`; 1334/1336: missing `weight_block_size` and `has_blocked_weights`. | Pre-existing; the Mia hunk is only the nvfp4-MLA validator at 2429-2431. |
| `vllm/v1/worker/gpu/model_runner.py` | 376: `float` passed to `format_gib`; 437/732/765/1321/1790: Pyright sees `Tensor` as non-callable; 528: `narrow` union mismatch; 602: invariant KV-cache list; 682/689/737-739/815/1145/1346/1703/1729/1795-1796/1851: existing optional or signature mismatches; 879: integer passed to boolean parameters; 1166/1250-1251: possibly unbound variables; 1753-1754 and 1860/1863: constructor keyword/signature mismatch; 1810: missing `draft_token_confidence_probs` on speculator variants. | Pre-existing; the only Mia hunks are the two `None` guards at 733 and 1791-1792. The adjacent 732/1790 callable reports point at unchanged `get_mtp_target_hidden_states()` calls. |
| `vllm/v1/engine/detokenizer.py` | 225: `TokenizerLike._tokenizer` may be `None`; 246: dynamic `Tokenizer.added_token_ids` assignment is unknown to Pyright. Hints at 43, 47, and 339 report unused parameters. | Pre-existing; assignments and base method signatures are unchanged. Mia factory/state additions introduce no diagnostic. |
| `vllm/config/speculative.py` | 373, 424, 435, 460, 477, 487, 498, 517, 630: `None` possibly subscripted. | Pre-existing; Mia additions are helper methods near the class tail and do not overlap these lines. |
| `vllm/engine/arg_utils.py` | 329: `default` possibly unbound; 449: dtype-to-`ModelDType`; 816/826: `Path` assigned to string fields; 1839: dict lacks `to_serializable`; 2071/2089: Ray attributes unknown; 2346: string-to-`RunnerType`; 2380: `lora_dtype` union; 2438: Mamba backend enum; 2519: string-to-`OffloadBackend`. | Pre-existing; Mia changes are scheduler argument wiring at 1535-1537 and `max_num_partial_prefills` plumbing, not these locations. |
| `vllm/model_executor/layers/sparse_attn_indexer.py` | 262/329/485/945: Triton kernel functions reported non-subscriptable; 1066/1070/1073/1076/1145/1299/1521/1524/1621/1624/1627/1630: unresolved `sparkinfer.attention.nsa_indexer` imports; 2088-2089: possibly unbound quantization buffers; 2236: missing `active_width`; 2454: `Tensor | None` return; hints at 2428-2449 report unused fake-op parameters. | Pre-existing except the unused-parameter hints, which are expected for the intentionally dormant Stage-A fake-op API. Added call-site keywords and dormant gate are type-compatible at runtime. |
| `vllm/v1/attention/backends/mla/flashmla_sparse.py` | 337/481/789: `NoReturn` not iterable; 379: `int | float` for decode count; 465: tensor for integer sequence length; 491: numeric union for integer query length; 744: tensor-or-tuple return assigned to tensor. | Pre-existing; Mia hunk only extends the dispatch tuple to include `nvfp4_ds_mla` near 865. |

`git diff --check` passes. Focused pytest collection remains blocked by the
environment failures recorded above (`nixl_ep.Buffer` missing and the
pre-existing `_C_stable_libtorch` fused-minimax symbol); no blocked test is
reported as passing.
